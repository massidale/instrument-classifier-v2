# Core deep learning
torch>=2.2
torchaudio>=2.2
numpy>=2.0

# Audio front-end (log-mel computed in-graph, as PANNs expects)
torchlibrosa>=0.1.0
soundfile>=0.12

# Training utilities / metrics / config
scikit-learn>=1.3
pyyaml>=6.0
tqdm>=4.65
tensorboard>=2.13
matplotlib>=3.7

# Dev / testing
pytest>=7.4
